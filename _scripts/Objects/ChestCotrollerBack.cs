using UnityEngine;
using System.Collections;

public class ChestCotrollerBack : MonoBehaviour {
	public Animator anim;

	// Use this for initialization
	void Start () {

	}
	
	// Update is called once per frame
	void Update () {
		/*if (GameManager.chestOpening) {
			anim.SetBool ("isOpening", true);
			GameManager.chestOpening = false;
		} else if (!GameManager.chestOpening) {
			anim.SetBool ("isOpening", false);
		}
		if( anim.GetCurrentAnimatorStateInfo(0).IsName("open") && !GameManager.chestOpen){
			GameManager.chestOpen = true;
		}

		if (GameManager.chestOpen) {
			anim.SetBool ("isOpen", true);
		} else if (!GameManager.chestOpen) {
			anim.SetBool ("isOpen", false);
		}*/
			
		
	}


}
